import 'package:flutter/material.dart';

class EjerciciosGimnasioScreen extends StatelessWidget {
  const EjerciciosGimnasioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Lista de ejercicios para hacer en el gimnasio',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
